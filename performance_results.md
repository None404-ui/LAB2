# Результаты тестирования производительности API

Этот файл перезаписывается автоматически после каждого запуска `scripts/run-newman.js`: скрипт прогоняет коллекцию Newman нужное количество раз, собирает статистику по каждому запросу и обновляет таблицу ниже.

## Последние доступные данные

| Эндпоинт | Среднее (мс) | Медиана (мс) | P95 (мс) | Мин (мс) | Макс (мс) | Успешность | Выборок |
|----------|--------------|--------------|----------|----------|----------|------------|---------|
| —        | —            | —            | —        | —        | —        | —          | —       |

> После самостоятельного запуска скрипта строки таблицы будут заменены фактическими метриками для всех REST-запросов.